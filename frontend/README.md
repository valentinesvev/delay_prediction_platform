# Интерфейс диспетчера

Место для будущего frontend. Стек и экраны пока не определены.

API принимает `POST /predict` с JSON `{"current_delay": 12.5}`
и возвращает `{"predicted_delay": 12.5}`. Задержки измеряются в минутах.
